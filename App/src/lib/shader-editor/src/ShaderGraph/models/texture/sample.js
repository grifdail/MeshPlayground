export const Sample = {
  name: "Sample texture",
  category: "texture",
  inputs: [
    {name:"texture", type:"sampler2"},

  ],

  toGLSL(inputs, params, outputs, inputsTypes)  {
    return ``;
  }
}

export default Sample;
